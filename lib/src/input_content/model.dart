enum FormLayout {
  // 行布局
  row,
  // 列布局
  column,
}

// 点击后置图标的事件枚举
enum SuffixIconEvent {
  // 纯点击
  onTap,
  // 显示密码
  showPassword,
  // 清空内容
  clear,
}

// 边框类型枚举
enum BorderType {
  // 默认边框
  border,
  // 正常边框
  enabledBorder,
  // 聚焦边框
  focusedBorder,
}
